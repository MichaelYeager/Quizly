name: quizly
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management (Kriteria 7)
  provider: ^6.1.2

  # Font Kustom (Kriteria 5)
  google_fonts: ^6.2.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Aset (Kriteria 4)
  assets:
    - assets/images/quiz_logo.jpg

  # Font (Kriteria 5)
  # (Saya menggunakan google_fonts, tapi jika Anda ingin manual, caranya seperti ini)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700